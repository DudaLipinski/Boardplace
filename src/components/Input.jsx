import styled from 'styled-components'
import { Input as AntInput } from 'antd-mobile'

export const Input = styled(AntInput)`
  background-color: var(--adm-color-neutral-800);
  padding: 13px 20px;
  --font-size: var(--adm-font-size-6);
  border-radius: 14px;
`
